export default function Employees({ name, role }) {
    return (
        <div>
            <h1>Employee Page</h1>

            <p>Name: {name}</p>
            <p>Role: {role}</p>
        </div>
    );
}